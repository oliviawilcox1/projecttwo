{% block content %}
<nav class="navbar navbar-expand-md navbar-dark bg-light">
    <div class="container-fluid">
        <a href="/" class="navbar-brand text-dark">Lyrical Fun! </a>
        {% if loggedIn %}
        <a href="/lyrics/mine" class="nav-link text-dark"> Hello {{ username | capitalize }}! </a>
        {% endif %}
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            {% if loggedIn %}
            <a href="/lyrics/new" class="nav-link text-dark">New Song Search</a>
            <a href="/lyrics/mine" class="nav-link text-dark">My Saved Lyrics</a>
            <a href="/lyrics" class="nav-link text-dark">View All Users Searches</a>
            <a href="/auth/logout" class="nav-link text-dark">Log Out</a>
            {% else %}
                <a href="/auth/signup" class="nav-link text-dark">Sign Up</a>
                <a href="/auth/login" class="nav-link text-dark">Sign In</a>
            {% endif %}
        </div>
    </div>
</nav>
{% endblock %}
